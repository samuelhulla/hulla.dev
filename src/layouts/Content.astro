---
import type { HTMLTag } from 'astro/types'

export interface Props {
  class?: string
  element?: HTMLTag
}

const { element: Element = 'main', class: className } = Astro.props
---

<Element class:list={['px-8 md:px-16', className]}>
  <slot />
</Element>
