---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'tbody'>

const { class: className, ...props } = Astro.props as Props
---

<tbody {...props} data-slot="table-body" class={className}>
  <slot />
</tbody>
